using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This enum should only include materials such as wood, ore and ingots. So anything that the player can have
/// more than one of should be included in this enum.
/// </summary>
public enum MaterialType 
{
	Wood, Ore 
}
